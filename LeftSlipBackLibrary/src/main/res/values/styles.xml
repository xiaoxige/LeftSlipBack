<resources>

    <!-- Base LeftSlipBack application theme. -->
    <style name="LeftSlipBackTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/colorTransparent</item>
        <item name="android:background">@color/colorDefaultBg</item>
        <item name="android:windowIsTranslucent">false</item>
    </style>

</resources>

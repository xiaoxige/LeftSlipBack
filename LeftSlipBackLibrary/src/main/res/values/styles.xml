<resources>

    <!-- Base LeftSlipBack application theme. -->
    <style name="LeftSlipBack.theme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="android:windowBackground">@color/colorTransparent</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

</resources>
